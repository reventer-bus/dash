DATABASE_URL=postgresql+asyncpg://makerai:makerai_dev@localhost:5432/makerai
REDIS_URL=redis://localhost:6379
SECRET_KEY=change-this-in-production-use-openssl-rand-hex-32
ACCESS_TOKEN_EXPIRE_MINUTES=1440
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=makerai
MINIO_SECRET_KEY=makerai_dev
MINIO_BUCKET=makerai-files
MINIO_SECURE=false
OCTOPRINT_BASE_URL=http://localhost:5000
OCTOPRINT_API_KEY=
ENVIRONMENT=development

# Shopify integration (store.fofus.in)
# Admin API token — create in Shopify Admin → Apps → Develop apps
SHOPIFY_DOMAIN=store.fofus.in
SHOPIFY_ADMIN_TOKEN=shpat_...
# Webhook HMAC secret — copy from Shopify Admin → Notifications → Webhooks
SHOPIFY_WEBHOOK_SECRET=
